# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.21.0
matplotlib>=3.5.0
tqdm>=4.64.0
einops>=0.6.0

# Optional dependencies for VAE and image processing
diffusers>=0.21.0
transformers>=4.30.0
Pillow>=9.0.0

# Optional dependencies for FID evaluation
clean-fid>=0.1.35

# Optional dependencies for development
pytest>=7.0.0
pytest-cov>=4.0.0
